# ShopBuddy

# CMPT 863- Group 10

This is a code bundle for Groceries list creaiton. The original project is available at https://www.figma.com/design/B8AdvMAk7SgPZKwf4DTws7/Groceries-list-creaiton.

## Running the code

Run `npm i` to install the dependencies.

  Create a local `.env` file and add your OpenAI API key:

  `VITE_OPENAI_API_KEY=your_openai_api_key_here`

If you change the env file while the dev server is running, restart `npm run dev`.

Run `npm run dev` to start the development server.
